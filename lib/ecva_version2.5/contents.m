%
% The ECVA Toolbox
% Version 2.5 - November 2010
% Copyright(c) Nørgaard & Bro, Chemometrics Group, University of Copenhagen, Denmark
%
% ECVA MODELS
%   ecva('model'):     Calculates an ECVA model
%   ecva('pred'):      Used for classification of new samples
%   ecva('iecva'):     Calculates an interval ECVA model
%   ecva('randtest'):  Test overfit for a given data set dimension
%
% ECVA PLOTS
%   ecva('cv1d'):      Makes 1D canonical variates plots
%   ecva('cv2d'):      Makes 2D canonical variates plots
%   ecva('cw1d'):      Makes 1D canonical weights plots (for e.g. spectroscopic data)
%   ecva('cw2d'):      Makes 2D canonical weights plots (for e.g. chemical data)
%   ecva('iplot'):     Plots results from interval ECVA model
%   ecva('icanonvar'): Plots canonical variates from interval ECVA model
%
% GENERAL TOOLS
%   script_manseg:     Example of a script for manual specification of segments for cross validation
%
% GETTING STARTED
%   ecva('demo1')  (uses DemoFluoData.mat)
%   ecva('demo2')  (uses DemoChemicalData.mat)
%   ecva('demo3')  (uses DemoInfraredMilk.mat)
%   contents.m (this file)
%   Read IMPORTANT_ReadMe.txt
%